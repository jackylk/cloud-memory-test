# AWS Bedrock Knowledge Base 存储后端架构对比

## 1. 概述

AWS Bedrock Knowledge Base 支持多种向量存储后端，本文档对比分析 **OpenSearch Serverless** 和 **Aurora PostgreSQL + pgvector** 两种主流方案。

## 2. 架构对比

### 2.1 OpenSearch Serverless 架构

```
┌──────────────────────────────────────────────────────┐
│         Bedrock Knowledge Base (OpenSearch)          │
├──────────────────────────────────────────────────────┤
│                                                       │
│  S3 Bucket                 Bedrock                  │
│  ┌─────────┐              ┌──────────┐             │
│  │ Docs    │─────────────▶│ Titan    │             │
│  │ (*.txt) │              │ Embed v2 │             │
│  └─────────┘              └────┬─────┘             │
│                                 │                   │
│                                 │ 1024-dim vectors  │
│                                 ▼                   │
│  ┌────────────────────────────────────────────┐    │
│  │   OpenSearch Serverless Collection        │    │
│  │                                            │    │
│  │   Index: bedrock-knowledge-base-index     │    │
│  │   ┌─────────────────────────────────┐     │    │
│  │   │  Vector Field (knn_vector)      │     │    │
│  │   │  - Dimensions: 1024             │     │    │
│  │   │  - Method: HNSW                 │     │    │
│  │   │  - ef_construction: 512         │     │    │
│  │   │  - m: 16                        │     │    │
│  │   └─────────────────────────────────┘     │    │
│  │                                            │    │
│  │   Text Field: AMAZON_BEDROCK_TEXT         │    │
│  │   Metadata: AMAZON_BEDROCK_METADATA       │    │
│  └────────────────────────────────────────────┘    │
│                                                       │
│  Retrieve API ────────────────────────────────▶      │
│  ◀──────────────────────────── Results              │
└──────────────────────────────────────────────────────┘

特点：
✅ 全托管，自动扩展
✅ 无需 VPC 配置
✅ k-NN 搜索优化
⚠️  最终一致性
⚠️  成本按 OCU 计费
```

### 2.2 Aurora PostgreSQL + pgvector 架构

```
┌──────────────────────────────────────────────────────┐
│         Bedrock Knowledge Base (Aurora PG)           │
├──────────────────────────────────────────────────────┤
│                                                       │
│  S3 Bucket                 Bedrock                  │
│  ┌─────────┐              ┌──────────┐             │
│  │ Docs    │─────────────▶│ Titan    │             │
│  │ (*.txt) │              │ Embed v2 │             │
│  └─────────┘              └────┬─────┘             │
│                                 │                   │
│                                 │ 1024-dim vectors  │
│                                 ▼                   │
│  ┌────────────────────────────────────────────┐    │
│  │   Aurora PostgreSQL Serverless v2         │    │
│  │   (in VPC)                                 │    │
│  │                                            │    │
│  │   CREATE TABLE bedrock_kb (               │    │
│  │     id UUID PRIMARY KEY,                  │    │
│  │     embedding vector(1024),               │    │
│  │     text TEXT,                            │    │
│  │     metadata JSONB                        │    │
│  │   );                                       │    │
│  │                                            │    │
│  │   CREATE INDEX ON bedrock_kb              │    │
│  │   USING hnsw (embedding vector_cosine_ops)│    │
│  │   WITH (m = 16, ef_construction = 64);    │    │
│  │                                            │    │
│  │   Extensions: pgvector 0.5.0+             │    │
│  └────────────────────────────────────────────┘    │
│         │                                           │
│         │ VPC, Security Group, Subnets             │
│         ▼                                           │
│  Retrieve API ────────────────────────────────▶      │
│  ◀──────────────────────────── Results              │
└──────────────────────────────────────────────────────┘

特点：
✅ ACID 事务支持
✅ 强一致性
✅ 完整 SQL 能力
⚠️  需要 VPC 配置
⚠️  管理复杂度较高
```

## 3. 详细对比

### 3.1 功能对比

| 功能 | OpenSearch Serverless | Aurora PostgreSQL |
|------|----------------------|------------------|
| **向量索引算法** | HNSW | HNSW / IVFFlat |
| **维度限制** | 16,000 | 2,000 (pgvector) |
| **距离度量** | Cosine, L2, L1 | Cosine, L2, Inner Product |
| **全文搜索** | ✅ Elasticsearch | ✅ PostgreSQL FTS |
| **结构化查询** | ⚠️ Limited | ✅ Full SQL |
| **事务** | ❌ No | ✅ ACID |
| **JOIN 操作** | ❌ No | ✅ Yes |
| **聚合分析** | ✅ Aggregations | ✅ SQL Analytics |

### 3.2 性能对比

| 指标 | OpenSearch Serverless | Aurora PostgreSQL | 说明 |
|------|----------------------|------------------|------|
| **查询延迟 (P50)** | ~50-100ms | ~100-200ms | 小规模数据集 |
| **查询延迟 (P95)** | ~200-300ms | ~300-500ms | 峰值性能 |
| **索引构建** | 快 | 中等 | 大批量写入 |
| **并发查询** | 高 (自动扩展) | 中-高 (ACU 限制) | 吞吐能力 |
| **扩展性** | 自动无限扩展 | 128 ACU 上限 | Serverless v2 |
| **冷启动** | 无 | ~30s | Aurora Serverless |

**测试结果（基于我们的实际测试）**：

```
OpenSearch Serverless (knowledge-base-opensearch-k7sjw):
  P50 延迟: 644ms
  P95 延迟: 1363ms
  QPS: 1.1
  成功率: 100%
  文档数: 100 (小学考试题)
```

### 3.3 成本对比

#### OpenSearch Serverless 定价
```
组件：
- OCU (OpenSearch Compute Units)
  - Indexing: $0.24/OCU-hour
  - Search: $0.24/OCU-hour
- Storage: $0.024/GB-month

最小配置：
- 2 OCU (indexing) + 2 OCU (search) = 4 OCU
- 月成本估算 = 4 OCU × $0.24 × 730 hours = ~$700/月

实际使用 (10GB 数据, 1000 QPS):
- 约 $800-1200/月
```

#### Aurora PostgreSQL Serverless v2 定价
```
组件：
- ACU (Aurora Capacity Units)
  - $0.12/ACU-hour (us-east-1)
- Storage: $0.10/GB-month
- I/O: $0.20/million requests
- Backup: $0.021/GB-month

最小配置：
- 0.5-1 ACU (按需)
- 月成本估算 = 1 ACU × $0.12 × 730 + 10GB × $0.10 = ~$88/月 + I/O

实际使用 (10GB 数据, 1000 QPS):
- 约 $200-400/月
```

**成本优势**：Aurora PostgreSQL 约为 OpenSearch 的 **25-50%**

### 3.4 管理复杂度

| 任务 | OpenSearch Serverless | Aurora PostgreSQL | 难度差异 |
|------|----------------------|------------------|----------|
| **初始设置** | ⭐ (简单) | ⭐⭐⭐ (复杂) | VPC配置 |
| **扩展配置** | ⭐ (自动) | ⭐⭐ (手动) | ACU设置 |
| **监控告警** | ⭐⭐ (CloudWatch) | ⭐⭐ (CloudWatch) | 相同 |
| **备份恢复** | ⭐ (自动) | ⭐⭐ (配置) | 快照管理 |
| **安全配置** | ⭐⭐ (策略) | ⭐⭐⭐ (VPC+SG) | 网络隔离 |
| **升级维护** | ⭐ (托管) | ⭐⭐ (计划) | 版本管理 |

### 3.5 适用场景

#### OpenSearch Serverless 适合：
✅ **快速原型开发**：无需网络配置
✅ **纯向量搜索**：专注检索性能
✅ **弹性工作负载**：流量波动大
✅ **全文搜索+向量**：混合检索
✅ **无运维团队**：完全托管
❌ 成本敏感场景：高流量下昂贵

#### Aurora PostgreSQL 适合：
✅ **成本优化**：长期稳定负载
✅ **数据分析**：需要复杂 SQL
✅ **关系数据**：多表关联
✅ **强一致性**：事务要求
✅ **已有 RDS 基础设施**：复用
❌ 小团队/无运维能力

## 4. 性能优化建议

### 4.1 OpenSearch Serverless 优化

**索引优化**：
```json
{
  "settings": {
    "index.knn": true,
    "index.knn.algo_param.ef_search": 512
  },
  "mappings": {
    "properties": {
      "embedding": {
        "type": "knn_vector",
        "dimension": 1024,
        "method": {
          "name": "hnsw",
          "parameters": {
            "ef_construction": 512,
            "m": 16
          }
        }
      }
    }
  }
}
```

**查询优化**：
- 使用合适的 k 值（top_k）
- 启用过滤后查询
- 批量查询合并

### 4.2 Aurora PostgreSQL 优化

**索引优化**：
```sql
-- HNSW 索引（推荐，快速但占内存）
CREATE INDEX ON bedrock_kb
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);

-- IVFFlat 索引（省内存，略慢）
CREATE INDEX ON bedrock_kb
USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);

-- 提高召回率
SET hnsw.ef_search = 200;
```

**连接池**：
```python
# 使用 RDS Proxy 或 pgBouncer
max_connections = 100
shared_buffers = '4GB'
work_mem = '128MB'
```

## 5. 迁移路径

### 5.1 从 OpenSearch 迁移到 Aurora

```python
# 1. 导出 OpenSearch 向量
from opensearchpy import OpenSearch
os_client = OpenSearch(...)
vectors = os_client.search(index='kb-index', size=10000)

# 2. 导入到 Aurora
import psycopg2
conn = psycopg2.connect(...)
for doc in vectors:
    cursor.execute("""
        INSERT INTO bedrock_kb (id, embedding, text, metadata)
        VALUES (%s, %s, %s, %s)
    """, (doc['id'], doc['embedding'], doc['text'], doc['metadata']))
```

### 5.2 从 Aurora 迁移到 OpenSearch

```python
# 1. 导出 Aurora 向量
import psycopg2
conn = psycopg2.connect(...)
cursor.execute("SELECT id, embedding, text, metadata FROM bedrock_kb")
vectors = cursor.fetchall()

# 2. 导入到 OpenSearch
from opensearchpy import OpenSearch
os_client = OpenSearch(...)
for row in vectors:
    os_client.index(
        index='kb-index',
        id=row[0],
        body={
            'embedding': row[1],
            'text': row[2],
            'metadata': row[3]
        }
    )
```

## 6. 决策树

```
需要选择 Bedrock KB 存储后端？
├─ 有专职 DBA/运维团队？
│  ├─ 是 ─► 考虑 Aurora PostgreSQL
│  └─ 否 ─► OpenSearch Serverless
│
├─ 需要复杂 SQL 查询？
│  ├─ 是 ─► Aurora PostgreSQL
│  └─ 否 ─► OpenSearch Serverless
│
├─ 成本敏感（高流量）？
│  ├─ 是 ─► Aurora PostgreSQL
│  └─ 否 ─► OpenSearch Serverless
│
├─ 需要事务支持？
│  ├─ 是 ─► Aurora PostgreSQL
│  └─ 否 ─► OpenSearch Serverless
│
└─ 追求最简单部署？
   ├─ 是 ─► OpenSearch Serverless
   └─ 否 ─► Aurora PostgreSQL
```

## 7. 推荐总结

### 默认推荐：**OpenSearch Serverless**
**理由**：
- 🚀 零配置启动
- 🔄 自动扩展
- 📈 专为搜索优化
- ⚡ 查询性能优秀

**适用 90% 的知识库场景**

### 特殊场景推荐：**Aurora PostgreSQL**
**理由**：
- 💰 成本敏感（长期稳定负载）
- 🔗 需要关联查询
- 📊 数据分析需求
- 🔒 强一致性要求

**适用企业级、数据密集型场景**

---

**文档版本**: 1.0
**创建日期**: 2026-01-31
**作者**: Cloud Memory Test Team
**审核状态**: ✅ 已完成
