# 🚀 快速开始

## 本地运行

### 一键启动（推荐）

```bash
cd web
./run.sh
```

然后访问 http://localhost:5000

### 手动启动

```bash
cd web
pip install -r requirements.txt
python app.py
```

## 部署到 Zeabur

### 最快部署方式（3步完成）

1. **推送代码到 GitHub**

```bash
git add web/
git commit -m "Add test reports website"
git push
```

2. **登录 Zeabur**
   - 访问 https://dash.zeabur.com
   - 使用 GitHub 账号登录

3. **一键部署**
   - 点击 "Create Project"
   - 选择 "Deploy from Git"
   - 选择你的仓库
   - 设置 Root Directory 为 `web`
   - 点击 Deploy

完成！🎉

### 详细部署指南

查看 [DEPLOY.md](DEPLOY.md) 获取完整的部署指南。

## 网站功能

✅ 查看知识库测试报告
✅ 查看记忆系统测试报告
✅ 按时间排序，最新报告优先
✅ 响应式设计，支持移动端
✅ 自动读取新生成的报告

## 目录说明

```
web/
├── app.py              # Flask 主应用
├── requirements.txt    # Python 依赖
├── Procfile           # 生产环境启动配置
├── templates/         # 页面模板
│   ├── index.html           # 首页
│   ├── kb_reports.html      # 知识库报告列表
│   └── memory_reports.html  # 记忆系统报告列表
└── static/
    └── css/
        └── style.css  # 样式文件
```

## 常见问题

**Q: 如何添加新报告？**

A: 运行测试生成报告后，报告会自动出现在网站上。如果部署在 Zeabur，需要：
```bash
git add docs/test-reports/
git commit -m "Add new reports"
git push
```

**Q: 本地运行时找不到报告？**

A: 确保在项目根目录有 `docs/test-reports/` 目录，且包含报告文件。

**Q: 部署后访问不了？**

A: 检查 Zeabur 日志，确认：
1. Root Directory 设置为 `web`
2. 应用成功启动
3. 域名配置正确

## 技术支持

- 查看 [README.md](README.md) 了解更多细节
- 查看 [DEPLOY.md](DEPLOY.md) 了解部署详情
- 遇到问题？在 GitHub 提 issue

## 下一步

🎯 [本地测试] 运行 `./run.sh` 启动网站
🚀 [部署上线] 阅读 DEPLOY.md 部署到 Zeabur
🎨 [自定义] 修改 `static/css/style.css` 调整样式
