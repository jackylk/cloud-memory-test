# 🚀 Zeabur 部署清单

## ✅ 部署前检查

### 1. 准备报告文件
```bash
cd web
./prepare_deploy.sh
```
- [ ] 已复制测试报告到 `web/reports/` 目录
- [ ] 确认报告数量正确

### 2. 本地测试（可选但推荐）
```bash
# 测试 Flask 应用
python app.py
# 访问 http://localhost:5000 确认正常

# 测试 Docker 构建（如果安装了 Docker）
./test_docker_build.sh
```
- [ ] Flask 应用本地运行正常
- [ ] Docker 构建成功（可选）

### 3. 提交代码
```bash
cd ..  # 回到项目根目录
git add .
git commit -m "Add web application for Zeabur deployment"
git push
```
- [ ] 已提交所有更改
- [ ] 已推送到远程仓库

## 🌐 Zeabur 部署步骤

### 1. 登录 Zeabur
- [ ] 访问 https://dash.zeabur.com
- [ ] 使用 GitHub 账号登录

### 2. 创建/选择项目
- [ ] 创建新项目或选择现有项目

### 3. 添加服务
- [ ] 点击 "Add Service"
- [ ] 选择 "Git"
- [ ] 选择你的仓库

### 4. 配置服务
- [ ] **Root Directory** 设置为 `web`
- [ ] 确认 Zeabur 检测到 Dockerfile
- [ ] （可选）检查环境变量设置

### 5. 部署
- [ ] 点击 "Deploy" 按钮
- [ ] 等待构建完成（约 2-5 分钟）
- [ ] 查看 Build Logs 确认无错误

### 6. 验证部署
访问以下 URL 确认正常：

```
https://your-app.zeabur.app/health
```
应该返回：
```json
{"status": "ok", "service": "cloud-memory-test-reports"}
```

- [ ] `/health` 端点正常
- [ ] `/` 首页显示正常
- [ ] `/kb` 知识库报告可访问
- [ ] `/memory` 记忆系统报告可访问

## 📊 部署成功指标

✅ **构建成功**
- Build Logs 显示 "Build succeeded"
- 没有错误信息

✅ **应用运行**
- Runtime Logs 显示 "Booting worker"
- 显示 "Listening at: http://0.0.0.0:PORT"

✅ **功能正常**
- 健康检查返回 200
- 首页可以访问
- 报告页面正常显示

## 🔧 常见问题快速修复

### 问题 1: 构建失败 - pydantic-core 编译错误
**解决**: ✅ 已修复
- 使用 Dockerfile 和 Python 3.11
- 只安装 web/requirements.txt

### 问题 2: 找不到报告文件
**解决**:
```bash
cd web
./prepare_deploy.sh
git add reports/
git commit -m "Update reports"
git push
```

### 问题 3: 端口错误
**解决**: ✅ 已修复
- Dockerfile 使用 `${PORT:-5000}`
- Zeabur 会自动设置 PORT 环境变量

### 问题 4: 路径错误
**解决**: ✅ 已修复
- Root Directory 必须设置为 `web`
- app.py 会自动检测报告路径

## 📝 部署后任务

### 1. 绑定域名（可选）
- [ ] 在 Zeabur 添加自定义域名
- [ ] 在 DNS 服务商添加 CNAME 记录
- [ ] 等待 DNS 生效

### 2. 启用自动部署
- [ ] 在服务设置中启用 "Auto Deploy"
- [ ] 选择监听的分支（main/master）

### 3. 设置监控告警（可选）
- [ ] 配置 Zeabur 的监控规则
- [ ] 设置告警通知

## 🔄 更新流程

每次生成新报告后：

```bash
cd web
./prepare_deploy.sh
cd ..
git add web/reports/
git commit -m "Update test reports $(date +%Y-%m-%d)"
git push
```

如果启用了自动部署，Zeabur 会自动重新部署。

## 📞 获取帮助

- 📖 Zeabur 文档: https://zeabur.com/docs
- 💬 Discord: https://discord.gg/zeabur
- 📧 GitHub Issues: 在你的仓库创建 issue

## ✨ 部署完成！

恭喜！你的测试报告网站已成功部署到 Zeabur！

🔗 分享你的网站链接:
```
https://your-app.zeabur.app
```

---

部署日期: ___________
部署人: ___________
网站 URL: ___________
